# SKEP — image credits

**There are no third-party images in this template, and no photographs at all.**

Every visual on all three pages is drawn as inline SVG in the page source, or
generated at runtime from the colony model:

| What | Where it comes from |
|---|---|
| The four seasonal landscapes behind the skep apertures (`#scene-winter`, `#scene-spring`, `#scene-flow`, `#scene-harvest`) | Hand-authored inline SVG in `index.html`, with the two reused scenes duplicated into `honey.html` and `visit.html` so each page stays self-contained |
| The skep outline, coil courses and entrance (`#skep-lines`) | Hand-authored inline SVG path data |
| The cut-away hive in the instrument | Inline SVG; the honey arch, brood nest and bee field are sized at runtime by `js/site.js` from `js/colony.js` output |
| Population ribbon, forager line, forage bloom bands | SVG paths generated at runtime from the model |
| `img/favicon.svg` | Hand-authored |
| `img/og.webp` | A 1200×630 screenshot of this template's own hero, rendered headless from the local preview and encoded to WebP |
| `img/apiary-skep-sm.webp` (hub card, in the repo's root `img/`) | A screenshot of this template's own homepage, downscaled to 480×300 |

Licensing position: nothing here is licensed from anyone, because nothing here
was taken from anyone. The two generated WebP files are screenshots of this
template's own output.

No AI-generated video or imagery was used, so `VIDEO-POLICY.md` does not apply
to this template and no video credits were spent on it.
