# Image credits — Zan.e

**There is no photography on this page.** No stock, no AI-generated imagery, no
third-party photography, and no pictures of the host — the visual identity is
built entirely from type, colour and CSS.

| File | Source | Used for |
|---|---|---|
| `img/favicon.svg` | Drawn for this site | Favicon |
| `img/og-1200.webp` | Screenshot of this page's own hero | Open Graph card |
| `../../img/zane-live-sm.webp` | Screenshot of this page's own hero | Hub gallery card |

Both `.webp` files are captures of the page itself, so they carry no imagery
the page does not already contain. Regenerate them from a fresh screenshot
whenever the hero changes.

The hero's vertical-video frame holds a **CSS standby slate** — colour bars, the
wordmark, and the live countdown — driven by the same Singapore clock as the
rest of the page. It is markup, not an image.

Photography is deliberately pending: the show's own stills will be added later.
Until then nothing on the page pretends to be a photograph.

## Fonts

- **Archivo** (variable, weight 100–900 + width 62–125%) — SIL Open Font License.
- **IBM Plex Mono** 400/500 — SIL Open Font License.

Both are self-hosted from `fonts/`; no font CDN is contacted.
